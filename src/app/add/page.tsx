'use client';

import {
  Box, Typography, Select, MenuItem, FormControl, InputLabel,
  Button, Alert
} from '@mui/material';

export default function Home() {
    return (
      <Box>
        Add a new Dataset!
      </Box>  
    );
}